// MV3 service worker for CareerTrack Job Clipper.
// TODO (next step): listen for a "start-connect" message from the popup,
// open /extension/connect in a new tab, watch for it to redirect to
// /extension/connected?code=..., then POST that code to
// /api/extension/exchange to retrieve and store the API key.
